services:
  - type: web
    name: chess-backend
    env: java
    region: frankfurt # Optional: Choose a region close to you
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/chess-backend-1.0.0.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
    plan: free
